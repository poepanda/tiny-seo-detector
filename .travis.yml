language: node_js
node_js:
  - "8"
install:
  - npm install -g codecov
  - npm install
script:
  - npm test
after_success:
  # Upload code coverage generated by jest to Coveralls for an online status
  - cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js